extends layout

block content
  h1= title
  p Welcome to #{title}

  each match in matches
    .match
      .time= match.gametime
      .status= match.status
      .name= match.name
      .result= match.result
